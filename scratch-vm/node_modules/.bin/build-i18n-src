#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../sidekick-l10n/scripts/build-i18n-src.js" "$@"
  ret=$?
else 
  node  "$basedir/../sidekick-l10n/scripts/build-i18n-src.js" "$@"
  ret=$?
fi
exit $ret
